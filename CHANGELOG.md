# Changelog

版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)（`主版本.次版本.修订号`），每次发布打对应的 Git tag（`vX.Y.Z`）。

> **版本号说明**：`v1.0.0`～`v1.4.9` 是早期未足够谨慎标注的历史版本（已发布 Git tag，按规则
> 保留不动）。从 `v0.5.0` 起改用 `0.x.y`——阿里云百炼 Token Plan 这个 Provider 仍处于快速试错
> 阶段，`0.x` 更准确地反映"尚未达到可对外承诺稳定性"的真实状态，待其稳定后再规划重新回到 `1.x`。

## [0.5.5] - 2026-09-01

### 进展与新发现

`v0.5.4` 的登录判定修复**确认生效**：真实登录后抓到的 Cookie 从 17 个（全是匿名追踪）增加到
31 个，出现了 `JSESSIONID` / `XSRF-TOKEN` / `login_aliyunid_ticket` / `login_aliyunid_csrf` /
`yunpk` 等真正的登录会话凭证，SEC_TOKEN 也首次成功取到（22 字符）。查询因此推进到了下一步。

### 新问题定位：原定的网关地址已不可用

用户现在遇到"百炼用量接口返回异常状态（HTTP 302）"。用真实登录态做了对照实验：

| 请求变体 | 结果 |
|---|---|
| 当前实现（Origin + Referer + form 带 SEC_TOKEN） | 302 → `err.taobao.com/error1.html` |
| \+ 浏览器 User-Agent + `X-Requested-With: XMLHttpRequest` | 完全相同 |
| \+ SEC_TOKEN 放进请求头（`sec-token` / `x-xsrf-token`） | 完全相同 |
| \+ SEC_TOKEN 放进 query string | 完全相同 |
| **对照组：伪造一个不存在的 action** | **完全相同** |
| **对照组：完全不带 Cookie** | **完全相同** |

两个对照组返回与真实请求**完全一致**，证明请求在到达业务逻辑之前就被网关/路由层统一拒绝，
与鉴权信息、请求头形式均无关——即最初需求文档里给出的
`bailian-cs.console.aliyun.com/data/api.json` 这个地址，对当前的百炼控制台已不适用。

### 本版改动（诊断）

- 网络监听改为**接口发现**：登录成功后不再立即关窗，先让页面继续运行 6 秒，把 Token Plan
  额度页真正调用的数据接口录下来。命中 `per1WeekPercentage` / `per1WeekResetTime` 目标字段
  的响应会被高亮标记（`*** TARGET FIELDS FOUND ***`），其余用量/订阅相关 API 调用也记录
  URL 与状态码。全程只记录 URL、HTTP 方法、状态码、响应体长度，**不记录响应体内容**。
- 登录成功后的状态提示改为"已登录，正在读取额度信息…"。

Core+Tests 241/241 通过。下一步依据真实接口地址重写查询逻辑。

## [0.5.4] - 2026-09-01

### 修复（找到了 SEC_TOKEN 取不到的真正根因）

用户反馈的关键现象定位了根因：**登录窗口打开后页面根本没有登录状态，但程序却判定"已登录"、
抓走 17 个 Cookie 后自动关窗**。所以问题从来不在"SEC_TOKEN 提取"，而在登录判定本身——
程序拿着一份未登录会话的无效 Cookie 去查询，自然永远拿不到 SEC_TOKEN，也查不出额度。

两个叠加的判定缺陷：

- **`ExecuteScriptAsync` 返回值判断写错**：该方法返回的是 **JSON 序列化后**的结果——脚本抛
  异常或返回 undefined 时，宿主拿到的是字面量 `null`（4 个字符），而不是 C# 的 `null`。
  早期代码用 `Trim('"').Length > 0` 判断"脚本是否返回了账号 ID"，把这个 `null` 当成了有效
  返回值，直接误判为已登录。现在改为让脚本返回约定标记（`YES:` / `NO` / `ERR`），宿主侧
  精确匹配前缀。
- **Cookie 兜底判定不成立**：原先"存在 `login_aliyunid_pk` 等 Cookie 即视为已登录"。实测这些
  Cookie 记录的是"该浏览器 Profile 上次登录过哪个账号"，属于历史残留，登出/过期后依然存在；
  `cna` / `isg` / `tfstk` 更是阿里巴巴全站的匿名追踪 Cookie，与登录无关。已完全移除该兜底，
  只信任页面真实渲染出的登录态。

新增 `WebViewScriptResult`（Core 层，纯字符串解析、可单测）承载这段判定逻辑，并补 11 个
回归测试，重点覆盖"JSON 字面量 `null` 绝不能被当成有效结果"这个原始缺陷。

### 改进

- 登录窗口新增实时状态提示（"等待登录…" / "已检测到登录状态…"）——此前窗口在未登录时
  静默自关，用户完全无从判断程序状态。
- 修正顶部提示文案：明确说明本窗口使用独立的浏览器数据、**不共享** Edge/Chrome 的登录状态，
  首次使用需要在这个窗口里登录一次（用户此前误以为在别处登录过就能直接用）。
- 首屏加载完成后隐藏"正在加载…"覆盖文字——它是悬浮在 WebView2 之上的，此前从不隐藏，
  会一直压在登录页面中央。

Core+Tests 241/241 通过。已用真实环境验证：未登录状态下登录窗口不再误判自关（此前必现）。

## [0.5.3] - 2026-09-01

### 诊断（网络层探测扩大范围 + 加计数器排查订阅本身是否生效）

- 上一版网络诊断（监听所有响应，检查内容里是否含 `SEC_TOKEN`）连续两次真实登录都没有生成
  诊断文件——但排除范围可能不够全：之前只检查 `Content-Type` 含 `json`/`text` 的响应，会漏掉
  `SEC_TOKEN` 如果内嵌在某个业务 JS 文件（`Content-Type: application/javascript`）里的情况。
  现在改为默认检查所有响应体，只跳过明显的图片/字体/音视频二进制资源。
- 新增响应计数器：把"迄今观察到的网络响应总数"写进 frame 诊断文件，用于排查
  `WebResourceResponseReceived` 事件订阅本身是否正常触发——如果这次诊断文件里这个数字仍是
  0，说明问题出在事件订阅本身，而不是内容里确实没有 `SEC_TOKEN`。
- `GetContentAsync` 单次探测失败时也会记录异常类型（不含内容）到诊断文件，排除"响应体读取
  本身失败、被 catch 吞掉"的可能。

Core+Tests 230/230 通过。SEC_TOKEN 来源仍未确认，本版本继续排查。

## [0.5.2] - 2026-09-01

### 修复

- **有效登录态被误判为"Console Cookie 已失效"**：`LooksLikeLoginPage`（判断拉取到的控制台
  页面是否其实是登录页而非正常内容）的关键词列表里有 `"window.location"`——这是几乎任何
  现代前端 bundle 都会出现的普通 JS 字符串，真实抓包验证：一个完全有效、内容完整（243KB，
  含 `CURRENT_PK`）的已登录页面同样会命中它，被错误归类成"已失效"，导致用户看到"Console
  Cookie 已失效（被重定向到登录页），请重新登录"这个具有误导性的提示——而实际登录态完全
  正常，问题出在别处（SEC_TOKEN 获取，仍在排查）。移除该关键词，只保留真正的登录页特征
  （`passport.aliyun.com`、中文提示语）。
- 新增 3 个回归测试：有效登录页不再误判、真实登录跳转正确识别、中文提示语正确识别。

修复后同一份真实登录态的查询结果从"AuthenticationExpired（需要重新登录）"变为准确的
"ProviderError/ResponseFormat（SEC_TOKEN 未取得）"——反映的是真实情况，而不是一个错误分类。

Core+Tests 230/230 通过。SEC_TOKEN 获取问题本身仍在排查中（见上一版的网络层诊断）。

## [0.5.1] - 2026-09-01

### 诊断（继续排查 SEC_TOKEN 获取不到）

- 上一版的 iframe 诊断结果已排除三种假设：SEC_TOKEN 不在主 frame 的 window/DOM 里，也不在
  已知的任何子 iframe 里（`smarter-engine` 和三个 `about:blank`），也不是已采集到的 17 个
  Cookie 中的任何一个名字（不含 SEC_TOKEN / token / csrf / xsrf 关键字）。
- 新增网络层诊断：监听 `WebResourceResponseReceived`，检查 JSON/文本类响应体里是否含有
  `SEC_TOKEN` 字符串——如果它是通过某次 XHR/fetch 请求拿到、只存在于页面脚本闭包变量里
  （不挂在 `window` 上），前几版的 DOM/变量遍历天然看不到，只有直接看网络流量才能定位。
  只记录命中时的 URL/Content-Type/长度，不记录响应体内容本身。

Core+Tests 227/227 通过。本版本仍是诊断性质，尚未确认 SEC_TOKEN 的真实来源。

## [0.5.0] - 2026-09-01

### 修复（继续排查百炼登录后拿不到 SEC_TOKEN）

- **SEC_TOKEN 可能被隔离在 iframe 里**：百炼控制台是微前端架构，Token Plan 子应用很可能渲染
  在独立的 iframe 中；如果 SEC_TOKEN 由子应用自己的脚本注入，只存在于那个 iframe 的
  window/DOM 里，在主 frame 执行的脚本天然看不到。登录窗口现在会订阅
  `CoreWebView2.FrameCreated` 持续收集子 frame 引用，提取 SEC_TOKEN 时主 frame 找不到就
  依次尝试每个已知子 frame。
- 主 frame 和所有子 frame 都提取不到时，写入一份不含任何 token/Cookie 真实值的结构性诊断
  文件（临时排查用），帮助确认真实根因而不是继续凭经验猜测。
- **已知问题**：本版本尚未在真实账号上验证 SEC_TOKEN 能否成功提取——上一轮验证发现用户
  测试用的是旧版发布包而非本次修复后的构建，实际效果待下一轮真实登录验证确认。

## [1.4.9] - 2026-09-01

### 修复

- **登录成功但拿不到 SEC_TOKEN**：实测确认百炼控制台是纯前端 SPA（efm-fe 异步微前端），
  SEC_TOKEN 是页面 JS 运行后才动态注入的全局变量，**不会出现在任何一次 GET 请求拿到的
  服务端渲染 HTML 里**——哪怕 Cookie 是完全有效的登录态。这意味着"从控制台页面 HTML
  正则提取 SEC_TOKEN"这条降级路径在架构上就注定拿不到值，之前的错误文案还把这种情况
  误判成"登录已失效"，误导用户反复重新登录也解决不了问题。
  - 登录窗口（WebView2）里提取 SEC_TOKEN 从"只试一次"改为最多重试 6 次（间隔 500ms）：
    "检测到已登录"（`CURRENT_PK` 出现）和"SEC_TOKEN 已注入"是两个独立的异步时机，
    前者可能先于 SPA 懒加载的业务模块完成之前发生，只试一次容易扑空；
  - HTML 提取兜底路径确实取不到时，错误文案改为如实说明"该值只能在登录时由浏览器动态
    生成"，并引导重新执行一键登录（登录时的提取才是唯一可靠路径），不再归类为
    "登录已失效"。

Core+Tests 227/227 通过。

## [1.4.8] - 2026-09-01

### 修复

- **百炼登录成功后卡片持续显示"需要重新登录"**：根因是 v1.4.7 的修复方向错了——当时为绕开
  Windows 凭据管理器单条凭据 2560 字节上限，改成"只收集查询网关相关的两个域
  （bailian-cs.console + www.aliyun.com）的 Cookie"，这本质上是在猜"哪些 Cookie 是查询必需
  的"，而实际漏掉了用户真正登录所在域 `bailian.console.aliyun.com` 自身的会话 Cookie，
  导致每次查询鉴权信息都不完整。这次改为正确的方向：
  1. `SecureCredentialStore` 新增 `SaveLarge`/`TryReadLarge`/`DeleteLarge` 分片存储——
     任意长度的内容都按字节预算切成多条凭据存储、读取时原样拼接，不再需要对内容做任何取舍；
  2. 登录窗口改为抓取 WebView2 登录会话里的**全部** Cookie（不限域名），完整保留交给分片存储；
  3. 百炼平台的 Cookie 存取改用分片存储；SEC_TOKEN 较短仍用常规存储。
- 之前版本保存的登录态使用旧的单条存储格式，读取路径已切换到分片存储，**需要用户在设置页
  重新走一次"一键登录"**才能生效（旧格式凭据不会自动迁移，可用"清除登录态"手动清理）。

新增分片存储的往返/覆盖清理旧分片/空值/删除等 6 个回归测试，Core+Tests 227/227 通过。

## [1.4.7] - 2026-09-01

### 修复

- **百炼登录成功后窗口消失、但额度卡片不出现**：根因是登录抓到的完整会话 Cookie 太长，用
  UTF-16 编码写入 Windows 凭据管理器时超出单条凭据 2560 字节上限，`CredWrite` 失败（Win32
  错误码 1783），异常又没被接住——Cookie 没保存成功，Provider 判定"未配置"，卡片按规则隐藏，
  表现就是"登录后什么都没出现"。四处修：
  1. Cookie 只收集查询网关真正会用的域（`bailian-cs.console.aliyun.com` + 父域
     `www.aliyun.com`），不再收集页面本地/埋点 Cookie，体积大幅减小；
  2. 登录抓到的 Cookie 和 SEC_TOKEN 改用 UTF-8 编码存入凭据管理器（ASCII 值体积减半，
     2560 字节上限内能装更多）；
  3. `SecureCredentialStore.Save` 增加显式大小校验，超限时给出可读错误（含字节数），
     而不是让 Win32 错误码裸奔；
  4. `LoginTokenPlanAsync` 增加异常兜底，登录态保存失败时在设置页给出明确提示。
- **登录时顺带从页面提取 SEC_TOKEN**：登录成功后从 WebView2 页面 JS 里提取 SEC_TOKEN 并保存，
  查询时优先使用（跳过每次从控制台 HTML 正则提取的不稳定路径），取不到时才降级为 HTML 提取。

Core+Tests 221/221 通过（新增 UTF-8 超长凭据往返、超限可读错误两个回归测试）。

## [1.4.6] - 2026-09-01

### 修复

- **百炼一键登录被误判为移动端（显示"暂不支持移动端体验"、无法登录）**：WebView2 登录窗口的
  DockPanel 子元素顺序写错了——Grid（WebView2 所在）不是最后一个子元素，DockPanel 只给它
  Dock.Left 的内容宽度，实测 WebView2 控件只有 166px 宽，百炼控制台据此把视口判定为移动端而
  拒绝显示桌面版。已把"取消"条移到 Grid 之前、让 Grid 作为最后一个子元素填充剩余空间；
  另外把导航延迟到 WebView2 拿到真实宽度（&gt;400px）后才执行，双保险避免任何"预布局小尺寸
  渲染"的可能。实测修复后页面以 884px 桌面视口正常加载百炼控制台（含"登录"按钮），
  "暂不支持移动端"提示消失。

## [1.4.5] - 2026-09-01

### 新增

- **阿里云百炼 Token Plan（个人版）额度查询（测试版）**：新增 "Alibaba Token Plan" 平台，查询 Token Plan
  个人版 7 天额度使用率与重置时间。由于 Token Plan 没有公开的用量 REST 接口，走控制台网关
  （非公开接口，可能随时变化）：
  1. 携带 Console Cookie 拉取百炼控制台页面，提取 `SEC_TOKEN`；
  2. POST 控制台网关 `zeldaHttp.apikeyMgr./tokenplan/personal/api/v2/usage`，解析
     `per1WeekPercentage`（7 天已用比例）与 `per1WeekResetTime`（重置时间，Unix 毫秒）；
  3. 面板卡片显示「7 天已用 / 7 天剩余」两个窗口与重置倒计时，复用现有卡片模板。
- **WebView2 一键登录（替代手工填 Cookie）**：引入 `Microsoft.Web.WebView2`，设置页「一键登录」
  在内置浏览器中登录阿里云百炼，登录成功后自动抓取登录态（Console Cookie）存入 Windows
  凭据管理器，并自动触发额度查询——用户不需要手工复制粘贴任何内容。登录态失效后卡片显示
  「需要重新登录」，回到设置页一键重新登录即可。

### 说明

- 新增的第三方依赖 `Microsoft.Web.WebView2`：这是实现"免手工 Cookie 的一键登录"的必要组件，
  Windows 11 通常自带其 Evergreen 运行时（与 Microsoft Edge 同源）。
- Console Cookie / `SEC_TOKEN` 只经 Windows 凭据管理器存取，不落盘、不写日志、不进异常消息；
  日志（`[AlibabaTokenPlan] ...`）只含查询步骤与百分比/时间等非敏感信息。
- **已知限制**：Token Plan 用量走的是未公开的控制台网关，字段/端点可能随官方调整；
  WebView2 一键登录的真实账号端到端验证（登录→抓登录态→查询出额度）尚未完成，等待真实账号
  登录验证后再更新说明。

## [1.4.4] - 2026-08-31

### 改进

- **Claude 限流后延长冷却，减少"请求过于频繁"复发**：`v1.4.2` 的 18 秒冷却只挡得住短时间内
  的重复触发，挡不住"这次被限流了，下一轮 5 分钟自动刷新照样撞上同一个限流窗口"。现在
  一旦某个平台的查询结果本身就是"请求过于频繁"，改用更长的冷却（默认 3 分钟）——这段时间内
  任何来源的刷新请求都直接复用这份"限流"快照（面板上仍然如实显示限流状态，不是假装正常），
  不会再去戳服务端，等大概率已经出了限流窗口才允许下一次真实请求。一旦查询成功过一次，
  立即恢复正常的 18 秒冷却，不会被上一次的限流状态一直拖长。新增单测覆盖限流延长冷却、
  冷却过期后恢复正常查询、成功后冷却恢复正常三种场景。
- **深色模式补全**：WPF 原生 TextBox / ComboBox / CheckBox / RadioButton / Slider /
  ContextMenu / MenuItem / ToolTip / Separator 之前都没有跟着 `Brush.*` 主题令牌走，用的是
  系统默认的白底黑字控件外观——之前只有卡片、按钮、文字这些手写样式覆盖了颜色，深色模式下
  设置页里的输入框、下拉框、勾选框、单选框会保持系统浅色外观，跟周围深色背景格格不入
  （"深色覆盖面不全"）。现在这些控件全部换成跟随主题的手绘模板（圆角输入框、自定义勾选/
  单选图标、去系统边框的下拉菜单等），作为全局隐式样式生效于整个应用，不需要逐处指定；
  设置页原来局部覆盖 RadioButton 外观（只设了间距和文字色）的样式已移除，改用全局模板。
  已用真实截图对比浅色/深色两种模式下的设置页确认无回归、深色下不再有色块违和。

Core+Tests 208/208 通过，Debug/Release 全解决方案 0 警告 0 错误。改造前后截图见
`docs/screenshots/2026-08-31-dark-mode-coverage/`。

## [1.4.3] - 2026-08-30

### 修复

- **严重：某些环境下启动直接崩溃，连托盘图标都不出现**：`v1.4.2` 发布后实测发现，某些虚拟/
  远程桌面显示驱动（本机复现环境：远程桌面会话）会把 `GetDpiForMonitor` 报告成"成功"但
  DPI 值是 0。上游 `MonitorService` 拿 0 去做除法，产出 `Infinity`/`NaN` 一路带进窗口位置计算，
  启动时恢复上次显示模式（`RestoreLastModeAsync`）触发一次配置保存，`System.Text.Json`
  拒绝把非有限浮点数写成 JSON 直接抛异常——这个异常发生在 `OnStartup` 同步执行期间，
  一旦抛出就会中断 `OnStartup` 本身，后面创建托盘图标、注册全局快捷键、启动时刷新全部
  统统不会执行：进程还活着（能在任务管理器里看到），但界面上什么都没有，托盘图标也没有
  （包括被 Windows 折叠进的隐藏图标区域）。四处堵：
  - `MonitorService` 把 DPI ≤ 0 当成查询失败同等对待，回退到 96；
  - `WindowPlacementCalculator.Correct` 对显示器数据和历史保存的坐标都做有限性校验，
    非有限值一律当成"没有这份数据"处理，不再把 NaN/Infinity 算进最终结果；
  - `AppSettingsStore.SaveCore` 的静默降级捕获范围补上 `ArgumentException`
    （JSON 序列化非有限浮点数正是抛这个），保存设置失败不该有任何情况能拖垮整个应用；
  - `App.OnStartup` 给"恢复上次显示模式"这一步单独加了 try/catch——托盘图标必须出现
    这条比显示模式子系统的任何内部问题都优先级更高，不能再被其它任何未预见的问题连累。
  新增单测覆盖：显示器数据非有限、历史保存坐标非有限、`AppSettingsStore.Save` 面对非有限
  数值不抛异常。已用真实编译的 exe 实测验证：托盘图标正常出现（含"折叠进隐藏图标区域"这种
  Windows 默认行为下的定位）、`crash.log` 在验证过程中没有新增记录。

### 改进

- **面板顶部"上次更新"时间不再可能比实际情况更新**：`v1.4.2` 加的刷新冷却生效后，"刷新全部"
  可能只有部分平台真的发了新请求、其余平台命中冷却返回的是旧快照，但头部时间之前是
  无条件盖成"现在"，会出现头部说"刚刚更新"但某张卡片其实还是十几秒前旧数据的不一致。
  现在头部时间直接从所有可见卡片自己的快照时间里现算，取其中最旧的一个，不再单独维护
  一个可能与实际不符的时间戳。

## [1.4.2] - 2026-08-30

### 改进

- **同一平台加了 18 秒的刷新冷却**：面板上能触发刷新的入口很多（定时自动刷新、手动按钮、
  托盘中键/双击、保存设置……），彼此互不知情，短时间内叠加很容易把某个平台的接口打到限流
  （尤其是 Claude 的非公开用量接口，经常出现"请求过于频繁"）。现在同一个平台刚查询完
  （无论成功失败）的 18 秒内，任何来源的刷新请求都直接复用上一次的结果，不再真的发新的
  网络请求；超过 18 秒的正常刷新不受影响。冷却按平台独立计算，自定义平台的接口地址被修改后
  会清空对应的冷却缓存，不会把改之前的旧结果多展示 18 秒。

## [1.4.1] - 2026-08-28

### 修复

- **MiniMax 周额度窗口被错误隐藏**：解析逻辑只在 `current_weekly_status == 1` 时才展示周额度窗口，
  误把这当成"该套餐没有周限额"的判断依据。实测（真实账号，周额度已用尽）发现该字段在周额度用尽时
  是 `2` 而不是 `1`，且 `current_weekly_remaining_percent` 仍是真实的 `0`——不是占位符，
  之前的逻辑会把这个真实的"周额度已用完"数据当成"没有周限额"整个隐藏掉，看起来就是
  "MiniMax 怎么只有 5 小时额度"。现在 `current_weekly_status` 为 `1`（正常）或 `2`（已用尽/受限）
  都会展示周额度窗口，只有实测中确认为无关占位的 `3`（在非套餐相关的 `video` 模型条目上观察到）
  继续视为不适用。新增单测覆盖这个真实场景（`ParseResponse_WeeklyStatusExhausted_WeeklyWindowStillIncluded`）。

## [1.4.0] - 2026-08-28

桌面看板模式重构：不再是"被压窄的托盘面板"，而是独立设计的紧凑看板。

### 变更

- **桌面看板换成独立紧凑视觉树**：不再复用完整的平台大卡片（`ProviderCardTemplate`），
  新增 `CompactProviderRowTemplate`——扁平一行，无卡片边框/阴影，用极细分隔线分隔平台。
  数据来自新增的只读展示适配层 `CompactProviderRowViewModel`（挂在
  `ProviderCardViewModel.Compact` 上），不重新解析任何 Provider 数据，跟标准视图共用同一份
  `Cards` 集合、同一套排序/可见性规则、同一套刷新命令。
- **默认尺寸（320×260）下四个平台同时完整可见，不再需要滚动**——重构前只能看到 1.5 个平台，
  需要滚动条才能看到 Codex/MiniMax/DeepSeek。
- **顶部区域大幅精简**：只保留 "QuotaFlow" + 紧凑更新时间（"刚刚 / 5 分钟前 / 数据过期"），
  移除版本号、"AI 额度状态"副标题、完整日期、周数、精确到秒的时钟、常驻多图标工具栏——
  这些常态不再占地方，工具栏改为鼠标移入后淡入（120～180ms）、移开约 650ms 后淡出。
  版本号和完整更新时间挪到 Tooltip。
- **平台行点名具体是哪个窗口异常**：Claude/Codex/MiniMax 每行显示 5 小时/7 天（或周）两个
  剩余百分比，异常时明确写"5 小时额度告急"/"周额度偏低"，不再是笼统的"紧张"；多个窗口
  异常时取全局最严重的一个说明，其余窗口详情在 Tooltip 里。DeepSeek 行显示币种+余额，
  不伪造 5h/7d 进度条。超过两个的额度窗口折叠为 "+N"，悬停/Tooltip 查看详情。
- **悬停工具栏**：刷新全部、始终置顶、位置锁定（图标随锁定状态切换🔒/🔓）、切换显示模式、
  更多菜单（打开设置/恢复默认窗口位置/切换标准-紧凑布局/退出桌面看板/退出应用）。
  工具栏视觉隐藏（Opacity 淡出）但不从可访问性树移除，键盘 Tab 仍可到达，屏幕阅读器/
  键盘用户不会被"看不见就摸不到"卡住。
- 拖动、边缘吸附、始终置顶、位置锁定、多显示器/DPI 位置恢复、启动恢复上次模式与位置全部保留；
  托盘弹出模式和悬浮窗口模式的视觉与交互未改动。

### 已知限制

- 未在真实 125%/150%/200% DPI 硬件缩放和系统高对比度模式下逐一截图验证——新增的紧凑视图
  沿用了应用现有的 DynamicResource 颜色/字体 Token 与相对单位布局（未引入任何硬编码颜色或
  绝对像素技巧），预期与既有页面表现一致，但未独立复核，建议后续在真实多 DPI/高对比度环境
  下人工确认。
- 深色主题下的紧凑视图未单独截图复核（验证脚本未能触发主题切走批量保存流程），同样因为
  复用既有颜色 Token 预期正常，但未独立证实。

## [1.3.1] - 2026-08-28

### 修复

- **"显示模式"切换按钮和"固定面板"按钮图标撞了**：托盘弹出模式的图标沿用了📌（图钉），跟旁边"固定面板"按钮的📌完全一样，两个功能不相关却长得一模一样，容易点错。托盘弹出模式的图标改为📥（收件箱/托盘造型，语义也更贴切），悬浮窗口🪟、桌面看板🖥️不变，三个模式图标和固定按钮的图标现在互不重复。

## [1.3.0] - 2026-08-28

显示模式：面板从"只能托盘弹出"扩展为三种可切换的窗口显示形态，不是简单补丁。

### 新增

- **三种显示模式**：面板顶部新增模式切换按钮（📌/🗗/🖥，循环切换），设置页新增"显示与窗口"卡片可直接选择：
  - **托盘弹出**（原有行为，默认）：点击托盘图标才出现，点外部自动收起，尺寸固定。
  - **悬浮窗口**：常驻显示，点外部不收起，可拖动，位置/大小独立记忆。
  - **桌面看板**：同悬浮窗口，但默认更小、默认不置顶、默认紧凑布局，适合长期摆在桌面一角。
  - 所有模式切换只重新配置窗口属性，不影响窗口当前是否显示——托盘单击/双击/全局快捷键/固定面板等既有交互不受影响。
- **每模式独立设置**：始终置顶、锁定位置（锁定后不可拖动）、紧凑布局、不透明度（70%–100%，托盘弹出模式恒定不透明）、背景材质（系统/云母/亚克力/纯色）、拖动吸附屏幕边缘、启动后是否恢复上次显示模式、悬停时临时提高不透明度以便查看——均在设置页"显示与窗口"卡片里，改动立即生效并持久化，不需要点"保存设置"。
- **拖动与吸附**：悬浮窗口/桌面看板模式下可通过顶部标题区域拖动（用 WPF 标准的 `Window.DragMove()`，不是手动模拟），松开鼠标时吸附屏幕工作区边缘（按住 Shift 临时关闭），拖动结束后才存盘一次，不是拖动过程中连续写盘。
- **多显示器 / DPI 感知的位置恢复**：悬浮窗口和桌面看板的位置、大小、所在显示器分别独立记忆；显示器被拔掉、分辨率变化、坐标跑到屏幕外时，会自动修正回一个当前一定可见的位置（标题栏区域至少可见），不会出现"窗口再也打不开因为它在一个不存在的屏幕上"的情况。设置页可一键"恢复默认窗口位置"。
- **云母 / 亚克力材质**：真实检测系统是否支持（Windows 11 22621+、非远程桌面、非高对比度），真实调用 DWM API 尝试应用；但面板窗口采用无边框自绘（`WindowStyle="None" AllowsTransparency="True"`）以实现圆角和阴影，这与 DWM 系统级合成材质存在架构性冲突（`AllowsTransparency` 用的是逐像素 Alpha 分层窗口，天然排斥 DWM 合成），因此云母/亚克力目前只能以对应色调的半透明背景色近似，而非真正的系统材质渲染——设置页会如实提示这一点，不假装做到。

### 修复

- 面板窗口曾经硬编码 `MinHeight="360"`（沿用自旧版单尺寸托盘面板），导致桌面看板模式的默认高度（260）被强制拉到 360——本次改为跟随窗口位置修正算法自身的最小尺寸（120×200），由实际运行时 UI 自动化验证发现并修复。

### 已知限制

- 悬浮窗口/桌面看板目前没有专门的"悬停展开工具栏"或进入/退出动画，属于本次范围内主动砍掉的部分，后续视需要再补。
- 无障碍适配仅限于既有 WPF 控件自带的能力和新增的 AutomationId，没有做专门的读屏软件走查。

## [1.2.0] - 2026-08-28

托盘交互体系升级 + 全局快捷键，不是简单补丁。

### 新增

- **托盘图标交互补全**：单击开关面板（不变）；**双击**打开面板并立即刷新全部；**中键**直接刷新全部，不用先打开面板；**右键**菜单精简为"刷新全部/设置/开机自动启动/退出"，"开机自动启动"改成菜单里的复选项，切换后有气泡提示确认（成功/失败都有反馈）。单击/双击的冲突（WinForms 的双击手势本身会先连续触发两次单击事件）用标准的延时消歧处理，不会出现"双击却先开又关"的观感问题。
- **面板固定**：面板顶部新增"固定"按钮，固定后点击面板外部不再自动收起，方便一边看额度一边操作其它窗口；通过托盘图标单击/双击、快捷键、Esc 手动关闭不受固定状态影响，固定只挡"点外部失焦"这一种收起方式。当次会话的临时状态，不持久化，每次重新打开面板默认未固定。
- **全局快捷键显示/隐藏面板**：默认 `Alt+Z`，可在设置页"快捷键"卡片改成任意"至少一个修饰键 + 主键"的组合（点击输入框后直接按下新组合即可，自动识别）。保存后立即生效；如果组合键被其它程序占用会提示注册失败，不会假装成功。

### 优化

- **设置页操作反馈更明确**：开机自动启动切换后不再只是笼统的"设置已保存"，会点名"开机自动启动已开启/已关闭"；万一注册表写入失败（权限问题等）也会明确提示失败，而不是静默什么都没发生却让用户以为设置生效了。

## [1.1.4] - 2026-08-28

### 修复

- **Codex 登录状态提示误导已登录用户**：Codex CLI 用 API Key 模式登录（而非 ChatGPT 订阅模式）时，面板卡片和设置页一直显示通用的"未检测到，请先运行 Codex CLI 并登录"，但用户其实已经登录过了，只是模式不支持查订阅额度——具体原因早就算出来了，只是被丢弃没有展示。现在会点明"Codex 当前未使用 ChatGPT OAuth 登录模式（可能是 API Key 模式），无法查询订阅额度"，不再误导。
- **设置页"保存设置"按钮要滚到最底才看得到**：设置页有十来张卡片，保存按钮之前和它们一起放在同一个可滚动区域的最后，每次改完设置都要先滚到底才能点保存。现在把"保存设置"按钮和状态提示挪到窗口底部一个固定的条状区域，跟卡片列表分离，不随内容滚动，随时可见随时可点。

### 说明

- **Claude/Codex 目前只识别对应命令行工具（CLI）的本机登录，识别不了 Claude Desktop / ChatGPT Desktop 客户端**——这是设计范围内的限制，不是 bug，详见 README「已知风险」第 5 条；退出 CLI 进程本身不影响检测，因为读的是登录时落盘的凭据文件，不依赖 CLI 是否在运行。

## [1.1.3] - 2026-08-28

### 修复

- **手动启动没反应**：v1.1.2 的"首次启动展示面板"只按"这台机器上有没有落过盘的设置文件"判断，同一台机器测试/使用过一次之后，后续任何手动双击都不会再展示，等于只在真正的全新安装上生效一次，体验上仍然是"点了没反应"。改为按启动方式判断：只要不是 Windows 开机自动拉起的（设置页"开机自动启动"注册的启动命令现在带 `--autostart` 标记），**每一次**手动启动（双击 exe、开始菜单、桌面快捷方式）都会展示一次面板；开机自启则保持安静，不会突然弹窗打扰。

### 说明

- 配置文件位置维持既有设计：`%LOCALAPPDATA%\QuotaFlow\settings.json`（DPAPI 加密），不会写在 exe 所在目录——这是有意为之，exe 本身是可放到任意目录运行的单文件可执行程序，用户数据统一放到系统规定的当前用户数据目录，不依赖 exe 所在路径可写、也不会因为移动/重装 exe 而丢失。

## [1.1.2] - 2026-08-28

### 优化

- **首次启动自动展示面板**：全新安装时只会多一个托盘图标，容易被当成"没装上/没启动"（图标默认还可能被 Windows 折叠进溢出区）。现在首次启动会像点击托盘图标一样自动展示一次面板，给用户一个"确实打开了"的直接反馈；点击外部照常自动收起，不强留。仅对真正的全新安装生效——已有配置文件的用户（含从旧版本升级）不受影响，也只会展示这一次，不会每次启动都弹。

## [1.1.1] - 2026-08-28

### 修复

- **已用/剩余切换时进度条不跟着变**：v1.1.0 面板顶部新增的"剩余/已用"切换按钮，此前只改了旁边的百分比数字，进度条固定按"剩余"填充不变——切到"已用"后数字变了、条却纹丝不动，看起来像没生效。现在条的填充量跟着当前显示的数字走（切"已用"时条按已用比例填充），颜色仍然按剩余量判断健康度不受影响。

## [1.1.0] - 2026-08-28

自定义平台数据模型升级，不是简单补丁。

### 新增

- **自定义平台支持多个额度窗口**：数据模型由"一个平台 → 一个额度指标"改为"一个平台 → 一次接口请求 → 多个额度窗口"。像 OpenCode GO 这种同时有 5 小时/每周/每月三个独立额度的平台，现在只需配置一个平台、一次接口地址，就能在同一张卡片里分别展示三个真实窗口——不再要求把它们拆成三个重复平台，也不再是笼统的一条"额度 100%"。
- **OpenCode GO 内置模板**：设置页"自定义平台"新增"OpenCode GO 模板"按钮，一键预填接口地址、Bearer 鉴权、以及 5 小时/每周/每月三个窗口的取值路径与重置时间路径，只需要填并保存 API Key。
- **每个额度窗口可独立配置数据语义**：已使用百分比、剩余百分比、余额、已使用数值（配额度上限）、剩余数值（配额度上限），互不影响，一个窗口出错只在它自己那一行显示"数据不可用"，不拖累同一平台的其它窗口。
- **窗口重置时间新增"剩余秒数"解析方式**：除了原有的 epoch 毫秒/秒数字和 ISO 8601 字符串，现在还支持接口直接返回"距重置还有多少秒"（`resetInSec` 语义），按响应到达时刻换算成绝对时间。
- **额度显示语义可在面板上直接切换**：面板顶部新增"剩余/已用"切换按钮，点击即可在"已用百分比"和"剩余百分比"两种念法之间切换，立即生效并持久化，纯展示层设置，不触发任何刷新或影响底层数据。

### 变更

- 自定义平台设置页的窗口编辑区改为可折叠列表：新增窗口默认展开，已保存窗口默认收起并显示摘要（数据语义 + 取值路径），最多支持 5 个窗口，同一平台内窗口名称不能重复，删除时至少保留一个。
- v1.0.5 保存下来的单窗口旧配置，加载时自动、原地迁移为新的多窗口格式：不丢平台配置、不丢 API Key（Id 不变，凭据键不变）、不产生重复平台、不需要用户重新配置；保存新格式前会先备份一份迁移前的信封快照（`settings.json.custom-quota-migrate.bak`），供意外情况下找回。

### 已知限制

- `ProviderSnapshot` 目前只支持单个余额指标：一个自定义平台里如果配置了多个"余额"语义的窗口，只有第一个成功解析的会显示为余额区块，其余会被静默忽略（该窗口本身若解析失败，会退化成一行"数据不可用"）。绝大多数场景（含 OpenCode GO）一个平台只会有一种数值形态，实际不会碰到这个限制。
- 本版本未在 125%/150%/200% 高 DPI 缩放下重新逐项截图验证新增的窗口编辑器 UI（沿用应用现有的 DPI 适配基础设施，未独立复测）。

## [1.0.6] - 2026-08-28

### 变更

- **卡片排序移到前台面板，从设置页移除**：每张卡片头部新增 ▲/▼ 小箭头，直接在托盘面板上调整顺序，点击立即生效并写盘持久化——不用再打开设置页、不用点"保存设置"。首位卡片的"上移"、末位卡片的"下移"自动置灰。设置页的"平台显示与顺序"整卡随之移除，减少了一屏配置项，也让顺序调整这件事一步到位（点几下箭头就完事，不用先切到设置页面）。

## [1.0.5] - 2026-08-28

### 修复

- **删除自定义平台不再是全设置页里唯一"不保存也生效"的操作**：此前点击「删除」会立即从 Windows 凭据管理器清除对应的 API Key，哪怕之后没点「保存设置」就关闭了设置窗口——这个 Key 依然永久丢失，与页面上其它编辑（顺序调整、新增平台、主题等）"不保存就等于没发生"的行为不一致，容易在误删/误关窗口时造成无法挽回的密钥丢失。现在删除只先把行从列表移除，真正清除凭据推迟到点击「保存设置」才执行——不保存直接关闭设置窗口，等于撤销这次删除（定义和 Key 都还在）。
- 相应修正了自定义平台 `custom-{n}` 编号的分配逻辑：改为按设置页会话自增（而不是扫描当前行列表求最大值），避免"先删除编号最大的行、再新增"在同一次会话里意外复用同一个 Id 而与尚未执行的挂起删除撞车。

### 优化

- 保存设置时若有自定义平台因配置不完整被跳过，状态提示改为**点名是哪个平台**（此前只笼统提示"有自定义平台配置不完整"，多个自定义平台时不知道该查哪一个）。
- 「自定义平台」卡片新增空态引导：一个自定义平台都还没添加时，显示"还没有自定义平台，点击右上角……试试"提示，而不是一片空白。
- 新增自定义平台时自动把新增的那一行滚动进可视区域——设置页较长，此前点击「+ 添加自定义平台」后新行可能出现在当前视口之外，容易让人以为没有反应。

## [1.0.4] - 2026-08-27

### 新增

- **配置驱动自定义平台**：设置页新增"自定义平台"卡片，可随时添加 / 删除其他额度平台（如 OpenCode、GO 或任意提供 JSON 接口的服务），无需改代码、重新编译或发版。每个自定义平台支持：
  - 三种鉴权方式：Bearer Token、自定义请求头（如 `X-API-Key`）、无需鉴权；
  - 三种额度语义：剩余百分比、使用百分比、余额（可配币种，如 CNY / USD）；
  - JSON 路径取值（点号属性 + 数组下标，如 `data.quota_left`、`data.items[0].quota`）与可选的重置时间字段路径；
  - 密钥同样只进 Windows 凭据管理器，配置加密持久化，删除平台时一并清除。
- **保存设置立即生效**：面板不再需要重启应用。新增 / 删除平台、改接口地址、调整顺序、改鉴权或 Key 后点保存，面板即时重建出对应的卡片（自定义平台带独立显示名），运行中直接反映变更。

### 优化

- 内置平台的"接口地址（可选）"覆盖也由"保存后重启生效"改为"保存后立即生效"，设置页相关提示文案同步更新。
- 内部：设置保存时实时重建 provider 列表并做卡片对账（保留仍存活的卡片实例、增删变化的卡片），同一平台并发刷新仍去重。

## [1.0.3] - 2026-08-27

### 新增

- **配置加密持久化**：`%LOCALAPPDATA%\QuotaFlow\settings.json` 改为 DPAPI（`DataProtectionScope.CurrentUser`）加密信封落盘，磁盘上只出现 `schemaVersion` / `cipher` / `payload`，不含任何明文配置。信封带 `schemaVersion`，旧版明文配置（v1.0.0~1.0.2）与旧版信封在加载时自动迁移，并保留旧文件快照 `settings.json.v0.bak` / `settings.json.vN.bak` 后再重写。
- **接口地址可配置**：设置页不再用独立的"接口地址（高级）"卡片，改为在每个平台自己的分组里提供"接口地址（可选）"输入框，并实时显示"当前使用：…"的生效地址（留空使用内置默认）。覆盖项同样加密持久化，保存后重启应用生效。
- **"已用尽"状态**：某个额度窗口剩余 = 0% 时，卡片状态显示"已用尽"（红色），与"紧张（告急，但还可继续使用）"明确区分——额度真的没了就是没了，不叫紧张。
- **API Key 输入合并**：MiniMax / DeepSeek 的密钥输入不再分"眼睛 + 独立输入框"两个控件，合并为单个输入框：已配置时默认显示掩码（只读，防止把掩码当 Key 误存），点眼睛并经 Windows Hello/密码验证后显示明文、可编辑；未配置时可直接输入。明文绝不常驻界面。
- **未配置的平台自动隐藏 + 顺序可调**：没有登录/没填 Key 的平台在面板上直接不显示（避免误读成 0%/0.00）；设置页新增"平台显示与顺序"卡片，用 ↑ / ↓ 调整面板卡片从上到下的顺序并持久化，保存后立即生效。
- **空面板友好引导**：所有平台都未配置时，面板不再是一张空纸，而是显示一句引导文案，告诉用户去设置页登录 Claude/Codex 或填写 MiniMax/DeepSeek 的 Key。
- **面板实时时钟**：托盘面板顶部新增实时日期/时间显示，格式支持"日期 + 周几 + 第几周 + 时间"（默认）、"日期 + 周几 + 时间"、"日期 + 时间"、"仅时间（实时）"四种，可在设置页"日期显示格式"切换并持久化。下拉里的每个选项都直接展示该格式的实时样本（每秒刷新），选哪个一眼看清，不再用"（完整）"这类后缀。
- **面板标题显示版本号**：产品名后自动显示当前版本号，与设置页"关于"同源（取自程序集版本），避免 UI 文本手工维护造成漂移。
- **诊断启动开关**：`--show-panel` 启动参数可在启动时直接展示面板且失焦不自动收起，供自动化检查使用；不带该参数行为与之前完全一致。

### 优化

- 设置页"关于"的版本号改为从程序集动态读取。
- 保存设置后立即触发一次后台刷新：改顺序、清除/新增 Key、开关项等立即反映到面板，不用等下一个自动刷新周期。

## [1.0.2] - 2026-08-27

### 修复

- **严重**：点击"眼睛"查看明文 Key 时，Windows Hello 未配置的机器上不弹任何验证框。
  根因有两层：① 当 Hello 报告"可用"但实际未给当前用户配置 PIN/生物识别时，
  `RequestVerificationAsync` 不弹 UI 直接返回，旧代码就此判定失败、从不降级到密码验证——
  现在统一降级到系统密码验证；② 密码验证用的 `CredUIPromptForWindowsCredentials` 因传了
  `hwndParent` 和 `CREDUIWIN_EXCLUDE_CERTIFICATES` 标志，在本机直接返回
  `ERROR_INVALID_PARAMETER` 导致弹窗根本不出现——现已移除这两处，凭据框正常居中弹出。
- 密码验证结果区分"已取消 / 验证失败"，不再把用户主动取消也算作失败。

### 优化

- 服务端新出现的未识别额度窗口（如 `nimbus_quill`）默认不再显示成英文名占位。
  新增设置项"显示未识别的新额度窗口"（默认关闭），开启后以中文标签（如"其他额度（nimbus_quill）"）
  展示，便于排查。
- 识别 Claude 新增的 `seven_day_omelette`（Claude Design 设计额度）窗口，中文显示为"7 天 (Design)"。

## [1.0.1] - 2026-08-27

### 修复

- **严重**：点击"设置"会导致整个托盘应用崩溃退出。根因是 `SettingsWindow.xaml` 里用
  `Icon="..."` 通过 XAML 类型转换器加载图标，在动态 `new` 出来的窗口（非 `StartupUri`
  主窗口）上会在 `InitializeComponent()` 阶段抛 `XamlParseException`；WPF 对 UI 线程的
  未处理异常默认直接终止进程。改为在代码后置里用 `BitmapImage` + pack URI 手动设置图标。
- 补上全局异常兜底（`DispatcherUnhandledException` / `AppDomain.UnhandledException` /
  `TaskScheduler.UnobservedTaskException`）：往后任何界面层未预料的异常都只会记录到
  `%LOCALAPPDATA%\QuotaFlow\crash.log`，不会再让托盘图标整个消失。
- 设置页密钥输入框禁用输入法（`InputMethod.IsInputMethodEnabled="False"`），避免第三方
  输入法钩子在这类只需要 ASCII 字符的输入框上触发冲突。

### 优化

- 托盘面板卡片间距进一步收紧（页脚"刚刚更新"行的留白从两段叠加的 14px 降到 2px）。

## [1.0.0] - 2026-08-27

首个可用版本。

### 新增

- Claude / Codex / MiniMax / DeepSeek 四平台额度查询，真实接口联调通过（详见 `README.md`）。
- Windows 系统托盘常驻，弹出面板展示四张平台卡片，固定顺序 Claude → Codex → MiniMax → DeepSeek。
- 本机 OAuth 凭据只读复用（Claude Code / Codex CLI），不要求重新登录、不收集账号密码。
- MiniMax / DeepSeek API Key 经 Windows 凭据管理器加密存储；设置页支持通过 Windows Hello（或密码兜底验证）查看已保存的明文 Key。
- 缓存先行展示 + 后台刷新；同一平台并发刷新请求自动去重；单平台失败不影响其他平台。
- 浅色 / 深色 / 跟随系统三态主题，125%/150%/200% DPI 适配。
- 品牌图标：渐变圆角背景 + "额度环"设计，覆盖托盘图标、应用图标、窗口图标。
- 71 个 xUnit 单元测试，覆盖额度换算、凭据解析、错误分类、缓存往返、并发去重等场景。

### 已知限制

- Claude/Codex 用量接口为非公开接口，未来可能变化（见 `README.md` "已知风险"）。
- 托盘面板定位为启发式算法，非 Shell 层面精确坐标查询。
- 视觉效果为手写 Fluent 风格模拟，未使用系统级 Mica/Acrylic 材质。
