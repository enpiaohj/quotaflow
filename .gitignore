# 构建产物
bin/
obj/
publish/
*.dll
*.exe
*.pdb

# Visual Studio / Rider
.vs/
.idea/
*.user
*.suo

# 测试
TestResults/

# 发布快照归档（release/vX.Y.Z/...）
release/

# 发布/凭据相关（本项目本来就不该往仓库里写这些，这里是双重保险，对应产品文档 §7.7）
# 注意：不要用 *credentials*/*secret* 这类宽泛通配符——项目里
# ClaudeCredentialReader.cs / SecureCredentialStore.cs 等本来就是合法源码文件名，会被误伤。
*.local.*
APIKeys.txt
*.log
.credentials.json
seed_keys.txt

# 跨设备迁移备份包（v0.9.0 起）。内含用口令加密的 API Key 与登录态——虽然是密文，
# 也绝不该进版本库：仓库一旦外泄就等于把密文和爆破机会一起交出去了。
*.qfbackup

# OS
.DS_Store
Thumbs.db
